    # [수정 전]
    # create_diagonal_gradient_pill(cvs_ctrl, 5, 2, 60, 26, 3, add_c[0], add_c[1], add_c[0], add_c[1], "Add New", add_c[2], on_add, f_weight="normal")
    # create_diagonal_gradient_pill(cvs_ctrl, 70, 2, 55, 26, 3, del_c[0], del_c[1], del_c[0], del_c[1], "Delete", del_c[2], on_delete, f_size=8, f_weight="normal")

    # ========================================================
    # [수정 후] 폰트를 볼드체로 변경
    create_diagonal_gradient_pill(cvs_ctrl, 5, 2, 60, 26, 3, add_c[0], add_c[1], add_c[0], add_c[1], "Add New", add_c[2], on_add, f_weight="bold")
    create_diagonal_gradient_pill(cvs_ctrl, 70, 2, 55, 26, 3, del_c[0], del_c[1], del_c[0], del_c[1], "Delete", del_c[2], on_delete, f_size=8, f_weight="bold")
    # ========================================================
